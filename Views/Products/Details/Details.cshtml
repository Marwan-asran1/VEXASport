@model VEXA.Models.Product

<div class="product-details-container">
    <div class="product-image">
        <img src="@Model.ImageUrl" alt="@Model.Name" />
    </div>
    <div class="product-details">
        <div class="product-header">
            <h1>@Model.Name</h1>
            <p class="price">EGP @Model.Price</p>
        </div>
        <div class="product-description">
            <p class="D">@Model.Description</p>
            <p><strong>Category:</strong> @if (Model.Category != null && Model.Category.Name != null) {@Model.Category.Name}else{<text>uncategorized</text> }</p>
            <p><strong>In Stock:</strong> @Model.StockQuantity</p>
        </div>
        <button onclick="addToCart(@Model.Id)" class="add-to-cart-btn ">
            Add To Cart
        </button>
    </div>
</div>
